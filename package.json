{
  "name": "chinese-story-web",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd web && npm install && npm run build",
    "start": "cd web && npm start",
    "dev": "cd web && npm run dev"
  },
  "dependencies": {
    "next": "14.2.5",
    "react": "18.2.0",
    "react-dom": "18.2.0",
    "@google/generative-ai": "^0.20.0"
  }
}
